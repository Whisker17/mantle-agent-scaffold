# Mantle MCP v0.1-core Audit Response

**Response Date:** 2026-02-28  
**Source Audit:** `specs/audits/v0.1/v0.1-core-audit.md`  
**Scope:** Verify each finding against current code, apply fixes for confirmed issues, and document disposition.

---

## 1. Summary

All **Important** findings (I1-I5) were verified and fixed.  
All **Minor** findings (M1-M6) were either fixed directly or resolved via equivalent implementation updates.  
Informational notes were reviewed; only N2 required a code comment update.

Verification after fixes:

- `npm run typecheck` -> pass
- `npm test -- --run` -> pass (`22/22`)

---

## 2. Finding-by-Finding Disposition

| ID | Auditor Finding | Verification | Action Taken | Status |
|----|------------------|--------------|--------------|--------|
| I1 | `mantle_getTokenPrices` accepts empty `tokens` | Confirmed | Added strict validation and typed error `INVALID_INPUT` in `getTokenPrices` | Fixed |
| I2 | Unused `zod` dependency | Confirmed | Removed `zod` from direct dependencies and updated lockfile | Fixed |
| I3 | Spec example uses old `getAllowances(tokens, spenders)` shape | Confirmed | Updated `specs/mcp-design.md` workflow example to `pairs: [{ token, spender }]` | Fixed |
| I4 | Tool dispatch is O(n) via `.find()` | Confirmed | Re-keyed `allTools` by MCP tool name and changed dispatch to `allTools[name]` | Fixed |
| I5 | Tool descriptions missing concrete examples | Confirmed | Enriched all v0.1 tool descriptions with `Examples:` and real Mantle addresses | Fixed |
| M1 | `getChainInfo` returns extra fields not listed in tool output schema docs | Confirmed | Updated `specs/mcp-design.md` output schema to include `wrapped_mnt` and optional `faucet_urls` | Fixed |
| M2 | Prompt handler throws raw `Error` | Confirmed | Changed to throw `MantleMcpError` with code `PROMPT_NOT_FOUND` | Fixed |
| M3 | `INVALID_INPUT` missing from error catalog | Confirmed (after I1) | Added `INVALID_INPUT` entry to error code table in `specs/mcp-design.md` | Fixed |
| M4 | Missing `dev` and `prepublishOnly` scripts | Confirmed | Added `dev` and `prepublishOnly` scripts to `package.json` | Fixed |
| M5 | Missing `vitest.config.ts` | Confirmed | Added explicit `vitest.config.ts` | Fixed |
| M6 | Sequential token/allowance reads | Confirmed | Reworked both paths to concurrent reads via `Promise.all` (non-sequential) | Fixed |
| N1 | `prefetchResources` no-op | Confirmed | No change (intentional for static v0.1 resources) | Accepted |
| N2 | `environment` vs `network` mapping could use comment | Confirmed | Added explicit mapping comment in `src/lib/registry.ts` | Fixed |
| N3 | `node:crypto` in token-list helper | Confirmed | No change (Node stdio target for v0.1) | Accepted |
| N4 | No `src/abis` directory | Confirmed | No change (v0.1 uses only ERC20 ABI under `src/lib/erc20.ts`) | Accepted |
| N5 | `server.ts` wiring cleaner than spec skeleton | Confirmed | No action needed | Accepted |

---

## 3. Concrete Changes

### Code

- `src/tools/token.ts`
  - Added empty-input guard for `getTokenPrices` with `MantleMcpError("INVALID_INPUT", ...)`
  - Expanded tool descriptions with concrete `Examples:` values
- `src/tools/index.ts`
  - Rebuilt `allTools` as MCP-name keyed map (`tool.name`)
- `src/server.ts`
  - Changed dispatch from O(n) find to O(1) keyed lookup
  - `GetPromptRequestSchema` now throws `MantleMcpError("PROMPT_NOT_FOUND", ... )`
- `src/tools/chain.ts`, `src/tools/registry.ts`, `src/tools/account.ts`
  - Enriched descriptions with concrete Mantle examples
- `src/tools/account.ts`
  - Converted `getTokenBalances` / `getAllowances` to concurrent read execution
- `src/lib/registry.ts`
  - Added clarifying comment for `sepolia -> testnet` registry mapping
- `package.json`
  - Removed direct `zod` dependency
  - Added `dev` and `prepublishOnly` scripts
- `vitest.config.ts`
  - Added explicit Vitest config

### Spec/Docs

- `specs/mcp-design.md`
  - Fixed allowance workflow example to `pairs` schema
  - Updated `mantle_getChainInfo` output schema with `wrapped_mnt` and optional `faucet_urls`
  - Added `INVALID_INPUT` to error catalog

### Tests

- `tests/token-tools.test.ts`
  - Added test: rejects empty token list (`INVALID_INPUT`)
- `tests/server-wiring.test.ts`
  - Added test: tool map keys equal MCP tool names (O(1) lookup contract)
- `tests/tool-descriptions.test.ts`
  - Added test: every v0.1 tool description contains `Examples:` and real address pattern

---

## 4. Verification Evidence

Commands executed after fixes:

```bash
npm run typecheck
npm test -- --run
```

Observed results:

- Typecheck: pass
- Test suite: pass (`8` test files, `22` tests)

---

## 5. Notes for Re-Review

- The dispatch-path issue (I4) is now structurally prevented by keyed tool map + test.
- The input-validation gap (I1) is now covered by unit test.
- Description requirement (I5) is now enforced by a dedicated test.
- Sequential read concern (M6) is addressed via concurrent execution in v0.1; multicall-specific optimization can still be introduced in v0.2 without changing API shape.
