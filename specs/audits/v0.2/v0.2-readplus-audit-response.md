# Mantle MCP v0.2-readplus Audit Response

**Response Date:** 2026-03-01  
**Source Audit:** `specs/audits/v0.2/v0.2-readplus-audit.md`  
**Scope:** Verify each reported item against current code, fix confirmed issues, and provide re-review evidence.

---

## 1. Summary

All reported **Critical**, **Important**, and **Minor** issues were verified as real in the codebase and fixed.

Post-fix verification:

- `npm run typecheck` -> pass
- `npm test -- --run` -> pass (`12/12` files, `44/44` tests)

---

## 2. Finding-by-Finding Disposition

| ID | Audit Finding | Verification | Action Taken | Status |
|----|---------------|--------------|--------------|--------|
| C1 | `getSwapQuote` `best` only used Agni | Confirmed | Implemented true `best` routing: queries Agni + Merchant Moe, compares outputs, returns better route, single-provider fallback warning | Fixed |
| I1 | `getPoolLiquidity` skipped USD derivation fallback | Confirmed | Added reserve-based USD derivation using injected token price lookup when provider USD is null | Fixed |
| I2 | `querySubgraph` missing response-size cap | Confirmed | Added byte-size accounting + configurable cap (`MANTLE_SUBGRAPH_MAX_RESPONSE_BYTES`), rejects oversized responses | Fixed |
| I3 | Local HTTP inconsistency (`localhost` allowed, `127.0.0.1` blocked) | Confirmed | Updated endpoint policy to allow loopback hosts (`localhost`, `127.0.0.1`, `::1`) when local-HTTP flag is enabled | Fixed |
| I4 | Prompt content too condensed vs spec | Confirmed | Expanded all 3 v0.2 prompts with structured sections, workflow depth, and reference tables | Fixed |
| I5 | `mantle_gasConfiguration` missing `CRITICAL:` prefix | Confirmed | Updated prompt description to `CRITICAL: ...` | Fixed |
| M1 | `server.ts` transport error said `v0.1` | Confirmed | Replaced with version-agnostic stdio-only message | Fixed |
| M2 | Same-token swap identity fallback | Confirmed | Removed identity fallback; now rejects same-token swaps with `INVALID_INPUT` | Fixed |
| M3 | `querySubgraph` warnings always empty | Confirmed | Added warning population for pagination indicator (`hasNextPage=true`) | Fixed |
| M4 | `probeEndpoint` runtime method enum not enforced | Confirmed | Added runtime allowlist enforcement (`eth_chainId`, `eth_blockNumber`, `eth_getBalance`) with typed `INVALID_INPUT` | Fixed |
| M5 | `getPoolLiquidity` could throw raw `BigInt` error | Confirmed | Wrapped reserve parsing in typed validation (`INVALID_INPUT`) | Fixed |
| M6 / NEW-1 | Double import in `server.ts` | Confirmed | Consolidated imports from `errors.js` | Fixed |
| M6 / NEW-2 | `getChainStatus` example semantics misleading | Confirmed | Rewrote examples to actual status outputs (block/gas context) | Fixed |
| M6 / NEW-3 | `getBalance` placeholder address example | Confirmed | Replaced with real 40-char Mantle address example and concrete output-style hint | Fixed |
| M6 / NEW-4 | `tool-descriptions.test.ts` regex too weak | Confirmed | Reworked test to validate full 40-char hex format for any addresses present | Fixed |
| M6 / NEW-5 | `PROMPT_NOT_FOUND` missing from error catalog | Confirmed | Added `PROMPT_NOT_FOUND` row to `specs/mcp-design.md` error catalog | Fixed |

---

## 3. Informational Items

| ID | Item | Disposition |
|----|------|-------------|
| N1 | DeFi default deps return null/empty without injected providers | Accepted (intentional DI-first design for v0.2 readplus scaffold) |
| N2 | No `src/abis` directory | Accepted (deferred; not required for v0.2 read-only scope) |
| N3 | `getLendingMarkets` `all` currently only Aave v3 | Accepted (correct for v1 scope) |
| N4 | Endpoint policy allows non-standard HTTPS ports | Accepted (v0.5 hardening scope) |
| N5 | Pool liquidity description used placeholder address | Fixed (description updated to real-token-address example, placeholder removed) |

---

## 4. Concrete Code/Spec Updates

### Runtime code
- `src/tools/defi-read.ts`
  - `best` provider routing across both DEXes
  - same-token rejection
  - reserve parsing hardening
  - USD valuation fallback from reserves + token prices
- `src/tools/indexer.ts`
  - response byte accounting
  - subgraph response-size cap (`MANTLE_SUBGRAPH_MAX_RESPONSE_BYTES`)
  - pagination warning emission (`hasNextPage=true`)
- `src/lib/endpoint-policy.ts`
  - consistent local loopback HTTP behavior under `MANTLE_ALLOW_HTTP_LOCAL_ENDPOINTS=true`
- `src/tools/diagnostics.ts`
  - runtime method allowlist enforcement for `probeEndpoint`
- `src/server.ts`
  - import hygiene + stdio transport message cleanup
- `src/prompts.ts`
  - expanded v0.2 prompt payloads
  - `CRITICAL:` gas prompt description
- `src/tools/chain.ts`, `src/tools/account.ts`
  - carry-forward example quality fixes

### Spec/docs
- `specs/mcp-design.md`
  - added `PROMPT_NOT_FOUND` to error catalog
  - added `MANTLE_SUBGRAPH_MAX_RESPONSE_BYTES` environment variable row

### Tests
- `tests/defi-read-tools.test.ts`
  - added regression coverage for `best` routing, same-token rejection, USD fallback derivation, typed reserve errors
- `tests/indexer-tools.test.ts`
  - added local loopback override test, pagination warning test, response-size cap test
- `tests/diagnostics-tools.test.ts`
  - added invalid-method runtime rejection test
- `tests/prompts-v0-2.test.ts`
  - added `CRITICAL:` and detailed prompt content checks
- `tests/tool-descriptions.test.ts`
  - strengthened address format validation logic

---

## 5. Verification Evidence

Commands executed after all fixes:

```bash
npm run typecheck
npm test -- --run
```

Observed results:

- Typecheck: pass
- Test suite: pass (`12` files, `44` tests)

---

## 6. Re-Review Readiness

v0.2 audit findings have been applied and verified. This response can be used directly for Claude Opus re-review.
